# AMP is on when CUDA supports it.
batch_size: 8
learning_rate: 3e-4
weight_decay: 0.01
max_steps: 1200
warmup_steps: 100
eval_interval: 100
eval_batches: 40
inference_benchmark_batches: 5
grad_clip: 1.0
checkpoint_interval: 2500
num_workers: 2
use_amp: true
